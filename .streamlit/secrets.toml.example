# Optional. Leave unset to use the local SQLite paper account.
# Configure this in Streamlit Community Cloud's Secrets panel for persistence.
DATABASE_URL = "postgresql://username:password@host:5432/database?sslmode=require"
